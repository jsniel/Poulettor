# Security Policy

Poulettor exposes a local Wi-Fi access point when the web portal is enabled.

Default credentials:

```txt
SSID: poulator2000
Password: nounette
```

Change credentials before using the device in a shared environment.

Do not expose the device to the public internet.
